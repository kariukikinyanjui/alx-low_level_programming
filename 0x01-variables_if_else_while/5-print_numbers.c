#include <stdio.h>

/**
 * main - Prints all single digit numbers of base 10 starting from 0.
 *
 * Return: 0 (Success)
 */
int main(void)
{
	int digit;

	for (digit = 0; < 10; num++)
		printf("%d", digit);

	printf("\n");

	return (0);
}
